import {StyleSheet} from 'react-native';
import COLORS from '../../theme/colors';

const widgetsStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLORS.WHITE,
  },
});
export {widgetsStyles};
